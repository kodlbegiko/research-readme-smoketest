# Owner actions not available through the current GitHub connection

## Repository metadata

1. Set description to: `Pilot study and conservative static preflight for research-software README install-to-first-use paths.`
2. Add topics: `research-software`, `reproducibility`, `readme`, `documentation`, `joss`, `python`, `benchmark`.

## v0.1.0 release

After the pull request is merged and CI is green:

1. Create tag `v0.1.0` on the merge commit.
2. Create release title `v0.1.0 — JOSS issue 122 root-README pilot`.
3. Use the `CHANGELOG.md` v0.1.0 section as release notes.
4. Attach the source archive generated by GitHub. No binary artifact is required; the package can be built from source with `python -m build`.
5. Do not describe the result as a prevalence estimate for all research software.
